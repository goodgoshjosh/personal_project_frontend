import React, { Component } from 'react';

import Routing from './Routing';

export default () => (<Routing/>)

export const apiURL = 'https://personalprojectdb-utzfyaryts.now.sh/api' //This is the now link that holds the database API